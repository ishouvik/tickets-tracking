<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? "#{t('site.name')} - #{yield(:title)}" : t('site.name') %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : t('site.description') %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  <body<%= content_for?(:body_class) ? " class=#{yield(:body_class)} " : nil %> <%= content_for?(:body_id) ? " id=#{yield(:body_id)} " : nil %>>
